import SwiftUI

struct ProfileView: View {
    var body: some View {
        Text("Profile Tab")
    }
}

struct ProfileView_Previews: PreviewProvider {
    static var previews: some View {
        ProfileView()
    }
}
